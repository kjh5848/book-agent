services:
  postgres:
    image: postgres:16
    container_name: metacoding_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: metacoding_db
      POSTGRES_USER: metacoding
      POSTGRES_PASSWORD: metacoding_pass
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U metacoding -d metacoding_db"]
      interval: 10s
      timeout: 5s
      retries: 5
