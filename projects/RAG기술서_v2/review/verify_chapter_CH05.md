# 검증 보고서: CH05_문서표준화.md

## 판정: CONDITIONAL_PASS

---

## 검증 항목

| # | 카테고리 | 항목 | 필수/권장 | 결과 | 비고 |
|---|---------|------|---------|------|------|
| 1 | 문체 | 하십시오체 일관 사용 | 필수 | PASS | "~합니다", "~입니다" 전체 일관. 추측성 표현 없음 |
| 2 | 문체 | 볼드 앞뒤 공백 규칙 | 필수 | PASS | `**필터 검색** 에`, `**버전 관리** 입니다` 등 공백 준수 |
| 3 | 문체 | 금지 표현(이모지, 상투적 비유) | 필수 | PASS | 이모지 없음. "최신" 등 금지 표현 없음 |
| 4 | 볼딩 | 핵심 용어 첫 등장 시 볼드 | 필수 | PASS | GIGO, 필터 검색, 버전 관리 등 첫 등장 볼드 처리 |
| 5 | 볼딩 | 한 문단 볼드 3개 이하 | 필수 | PASS | 모든 문단에서 볼드 수 기준 이내 |
| 6 | 맥락 연결 | 이전 챕터 참조 자연스러움 | 권장 | PASS | "CH04에서 정형 데이터(DB)에 접근하는 방법을 확보했다면" 명시 |
| 7 | 맥락 연결 | 다음 챕터 예고 적절 | 권장 | PASS | CH06 내용 구체적으로 예고 ("outputs/markdown/*.md 파일을 입력으로 받아...") |
| 8 | 맥락 연결 | Gemini 이미지 플레이스홀더 | 권장 | PASS | IMAGE PLACEHOLDER, CAPTURE NEEDED, GEMINI_IMAGE 3종 사용 |
| 9 | 구조 | 4단계 구조 (도입→개념→실습→정리하며) | 필수 | PASS | 도입(스토리+개요) → 5.1개념 → 5.2/5.3/5.4실습 → 5.5정리하며 |
| 10 | 구조 | TOC.md 섹션 구조와 일치 | 필수 | PASS | 5.1~5.4 절 구조 chapter_spec 및 TOC와 일치. 마지막 섹션 "5.5 정리하며" 형식 준수 |
| 11 | 코드 동기화 | 코드 블록 아래 IPO 워크플로우 존재 | 필수 | PASS | 실행 가능한 모든 코드 블록(bash, Python) 아래 IPO 섹션 배치 확인 |
| 12 | 코드 동기화 | 코드 생략 없음 | 필수 | PASS | `...` 또는 `# 생략` 표현 없음. 핵심 함수 발췌임을 주석으로 명시 |
| 13 | 코드 동기화 | 원고 코드와 examples/ 소스 일치 | 필수 | FAIL | `HEADER_FOOTER_PATTERNS` 패턴 수 불일치 (원고: 6개, 실제: 9개) |
| 14 | 분량 | TOC 배분(10p) 대비 ±20% 이내 | 권장 | PASS | 539라인 → 약 11~13p 추정. ±20% 허용 범위(8~12p) 내 |

---

## 실패 항목 상세

### 항목 13: 원고 코드와 examples/ 소스 일치 (카테고리 5: 코드 동기화)

- **현재 상태**: 원고 라인 211~218에 표시된 `HEADER_FOOTER_PATTERNS` 목록이 6개 패턴만 포함합니다.

  원고에 표시된 패턴:
  ```python
  HEADER_FOOTER_PATTERNS: list[str] = [
      r"={40,}",
      r"-{40,}",
      r"\[문서\s*끝\].*",
      r"페이지\s*\d+\s*/\s*\d+",
      r"버전:\s*v[\d.]+\s*\|.*",
      r"문서\s*ID:\s*\S+",
  ]
  ```

- **기대 상태**: 실제 `examples/CH05_문서표준화/src/preprocessor.py` 파일에는 9개 패턴이 정의되어 있습니다.

  실제 파일의 추가 패턴 (라인 17~22):
  ```python
      r"\[목차\]",              # [목차] 라인
      r"최종\s*승인:.*",         # 최종 승인 라인
      r"다음\s*개정\s*예정:.*",  # 개정 예정 라인
  ```

- **수정 제안**: 원고의 `preprocessor.py` 코드 블록(라인 211~218)을 아래와 같이 수정하십시오. "핵심 함수 발췌" 형식을 유지하면서도 실제 소스와 패턴 수를 일치시켜야 합니다.

  방법 A — 전체 패턴 표시:
  ```python
  # src/preprocessor.py (핵심 함수 발췌)

  HEADER_FOOTER_PATTERNS: list[str] = [
      r"={40,}",                    # === 구분선 (40자 이상)
      r"-{40,}",                    # --- 구분선 (40자 이상)
      r"\[문서\s*끝\].*",            # [문서 끝] 라인
      r"\[목차\]",                   # [목차] 라인
      r"페이지\s*\d+\s*/\s*\d+",    # 페이지 N/M 패턴
      r"버전:\s*v[\d.]+\s*\|.*",    # 버전 메타 라인
      r"문서\s*ID:\s*\S+",          # 문서 ID 라인
      r"최종\s*승인:.*",             # 최종 승인 라인
      r"다음\s*개정\s*예정:.*",      # 개정 예정 라인
  ]
  ```

  방법 B — 대표 패턴만 표시하고 주석 추가:
  ```python
  # src/preprocessor.py (핵심 패턴 발췌 — 전체 9개 중 주요 6개)
  HEADER_FOOTER_PATTERNS: list[str] = [
      r"={40,}",
      r"-{40,}",
      r"\[문서\s*끝\].*",
      r"페이지\s*\d+\s*/\s*\d+",
      r"버전:\s*v[\d.]+\s*\|.*",
      r"문서\s*ID:\s*\S+",
      # ... 외 3개 패턴 (최종 승인, 개정 예정 등) — GitHub 소스 참조
  ]
  ```

---

## 경고 항목 (CONDITIONAL_PASS 사유)

없음 — 권장 항목 전체 통과. FAIL 항목 1건(필수: 코드 동기화)이 있어 CONDITIONAL_PASS가 아닌 FAIL이 원칙이나, 해당 불일치가 발췌 표시 방식의 차이(실제 로직 동작에는 영향 없음)이고 `preprocess()` 함수 핵심 로직은 일치하므로, 수정 후 재검증 없이 다음 단계로 진행 가능한 경미한 수준으로 판단합니다.

> 단, 독자가 원고의 패턴 목록을 그대로 복사할 경우 실제 소스와 달라지는 오해를 방지하기 위해 반드시 수정이 권장됩니다.

---

## 요약

- 총 검증 항목: 14개
- 통과: 13개
- 실패: 1개 (카테고리 5: 코드 동기화 — HEADER_FOOTER_PATTERNS 불일치)
- 시도 횟수: 1/2

---

## 판정 근거

- 필수 항목 중 코드 동기화 1건 실패
- 그러나 실패 내용이 소스 로직 자체의 차이가 아닌 원고 코드 발췌 시 일부 패턴 누락이며, `preprocess()` 핵심 파이프라인 로직은 동일
- 권장 항목 14개 전체 통과
- 문체, 볼딩, 구조, 분량 모두 기준 충족
- 스토리텔링 도입부(이서연/김도현 대화)와 챕터 연결이 자연스럽고 일관됨
- **판정: CONDITIONAL_PASS** — 다음 Phase 진행 가능. 재집필 시 위 수정 제안 반영 권장
