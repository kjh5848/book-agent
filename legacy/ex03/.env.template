# LLM Provider 설정 (ollama 또는 openai)
LLM_PROVIDER=ollama

# LLM 모델명 설정
# Ollama 예시: deepseek-r1:8b, llama3, mistral
# OpenAI 예시: gpt-4o, gpt-4o-mini, gpt-3.5-turbo
LLM_MODEL_NAME=deepseek-r1:8b

# Ollama 설정 (PROVIDER가 ollama인 경우 필요)
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI 설정 (PROVIDER가 openai인 경우 필요)
# OPENAI_API_KEY=sk-proj-...

# 데이터베이스 설정 (PostgreSQL)
DATABASE_URL=postgresql://metacoding:metacoding1234@localhost:5432/metacoding_db

# 벡터 DB 경로 
VECTOR_DB_DIR=data/embedding_db
