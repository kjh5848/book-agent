# 100페이지 기술서 및 실습서 집필 가이드: 에이전트 파이프라인 및 검증 체계

본 문서는 100페이지 이하의 기술서 및 실습서를 효율적으로 집필하기 위한 AI 에이전트 파이프라인 기획서입니다. 각 단계별로 배치된 에이전트의 명확한 **세부 산출물(Deliverables)** 과 이를 검사하는 **검증 에이전트(Verification Agent)** 의 필수 검증 항목을 상세히 정의합니다.

---

## 1. [기획 파트] 기획 및 환경 설계

### 📝 기획 에이전트 명세 및 산출물
**역할:** 전체 도서의 방향성 수립, 실습 환경 설계, 아키텍처 다이어그램 및 페르소나 설정

**상세 산출물 (기획 문서 그룹 등):**
**1. 설계서 (Project Blueprint)**
- 독자 페르소나 (대상 독자, 사전 지식 수준, 필수 선수 실습 환경)
- 챕터별 학습 목표 (Learning Objective) 및 최종 달성 기준
- 기술 스택 선정 및 명확한 버전 명세 (예: Python 3.11, LangChain 0.2)
- 실습 전 체크리스트 및 사전 준비 작업 가이드 (Door-opening guide)

**2. 아키텍처 및 다이어그램 (Architecture & Visuals)**
- 전체 시스템 구성도 (Mermaid 포맷)
- 챕터별 핵심 개념 ↔ 코드 모듈 매핑 테이블
- 시스템/모듈 간 의존성 그래프
- 시나리오별 실행 흐름도 및 데이터 처리 흐름도(Data Flow Diagram)

**3. 환경 명세 및 예외 기획 (Environment & Edge Cases)**
- 필수 환경 변수(.env) 목록 및 획득 방법 가이드
- 연동할 외부 API/서비스 목록 (무료/유료 여부, 요금제 한도 등)
- 지원 OS 범위 (macOS / Windows / Linux) 및 환경별 특이사항
- 실습 중 독자가 겪을 수 있는 주요 실패 시나리오 및 대응 방안 기획

**4. 분량 및 구성 계획 (Paging Plan)**
- 챕터 수 및 예상 페이지 상세 표 배분 (총 100페이지 이하 철저)
- 이론 vs 실습 비율 기획 (예: 이론 30%, 실습 70%)

### 🔍 기획 검증 에이전트 체크리스트
기획 단계에서 방향성이 잘못되면 프로젝트 전체가 흔들립니다. 기획 검증 에이전트는 다음을 철저히 검사합니다.

1.  **분량 및 타겟 적합성 검증:**
    *   기획 내용이 '100페이지 이내'라는 분량 제약에 맞게 너무 방대하거나 빈약하지 않은지(Scope Creep 방지) 확인합니다.
    *   설정된 대상 독자의 사전 지식 수준에 맞는 난이도로 기획되었는지 평가합니다.
2.  **실습 환경 명세 검증:**
    *   OS, 언어 버전, 주요 라이브러리 및 도구의 버전 정보가 기획서에 명확히 고정(Pinned)되어 있는지 검사합니다.
3.  **아키텍처 및 논리적 오류 검출:**
    *   제안된 아키텍처나 시스템 구조에 기술적 모순이 없는지 확인합니다.
4.  **트러블슈팅 및 엣지 케이스 고려 여부:**
    *   독자가 겪을 수 있는 잠재적 오류 상황에 대한 대비책이 기획에 포함되어 있는지 검증합니다.

---

## 2. [코드 파트] 실습 코드 및 프로젝트 구현

### 📝 코드 에이전트 명세 및 산출물
**역할:** 기획서 내용에 맞춘 실제 코드 구현, 책 내용 삽입을 위한 단위 추출, 에러 대응 자가 검증

**상세 산출물 (`examples/CH{번호}_{제목}/` 및 메타데이터):**
**1. 프로젝트 디렉토리 구조**
```text
CH{번호}_{제목}/
├── README.md             ← 챕터 요약 및 실행 가이드
├── requirements.txt      ← 패키지 버전 완전 고정
├── .env.example          ← 실제 키 미포함, 템플릿 제공
├── src/                  ← 실제 동작하는 소스 코드
│   ├── __init__.py
│   ├── main.py
│   └── {모듈}.py
├── data/                 ← 실습용 샘플 데이터 제공
├── tests/                ← 자가 검증용 단위 테스트 코드
│   └── test_{모듈}.py
├── snippets/             ← [New] 책 본문에 즉시 삽입할 수 있도록 논리 단위로 분할된 코드 조각들
├── logs/                 ← [New] 코드 실행 결과의 터미널 텍스트 캡처본 (본문 결과 묘사용)
└── outputs/              ← 코드 실행 결과 산출물 (gitignore 처리)
```

**2. 파일별 세부 산출물 명세**
- **단위 테스트 리포트:** `tests/test_report.md` (코드가 정상 동작함을 자동 증명한 내역)
- **실행 로그 문자열:** `logs/console_output.txt` (독자가 자신의 실행 결과와 직접 비교할 수 있도록 제공)
- **분할 스니펫:** `snippets/01_data_load.py` 등 (설명이 필요한 핵심 로직만 발췌한 조각 코드)
- **README 생성 항목:** 챕터 목적, 실행 환경, 설치 방법, 튜토리얼 순서, 구조 설명 등 

### 🔍 코드 검증 에이전트 체크리스트
독자가 직접 실행해 보는 책이므로, 코드의 완결성과 재현성 검증이 핵심입니다.

1.  **실행 가능성 및 재현성 (Reproducibility) 테스트:**
    *   제시된 실행 순서대로 빈 환경(Clean Environment)에서 코드를 실행했을 때, 의도한 터미널 출력값이 나오고 에러 없이 동작하는지 시뮬레이션 검증을 수행합니다.
2.  **요구사항 충족도 확인:**
    *   기획서에 명시된 모든 기능과 아키텍처가 실제로 코드에 구현되었는지 1:1로 대조합니다.
3.  **다이어그램 문법 및 포맷 규칙 검사:**
    *   Mermaid 다이어그램 작성 시, 괄호 `()` 가 포함된 텍스트 노드가 반드시 쌍따옴표 `""` 로 감싸져 있는지 문법 오류를 체크합니다. (예: `id["텍스트 (설명)"]`)
4.  **주석 및 코드 스니펫 규칙 적용 검사:**
    *   코드 내의 **모든 주석이 한국어로 작성**되었는지 철저히 확인합니다.
    *   본문 집필을 위해 코드 스니펫(`snippets/`) 파일과 실행 캡처본(`logs/`)이 올바른 규격으로 생성되었는지 확인합니다.

---

## 3. [집필 파트 - 목차] 구조 및 뼈대 설계

### 📝 목차 에이전트 명세 및 산출물
**역할:** 확정된 기획서와 리드미(코드 로직)를 바탕으로 교육학적 관점의 도서 목차 생성

**상세 산출물 (`outline/TOC.md`):**
**1. 전체 구조도 (Table of Contents)**
- 대분류(Part) - 중분류(Chapter) - 소분류(Section) 계층 구조
- 각 챕터 하위의 주요 섹션 요약(Abstract) 및 각 섹션별 예상 페이지 수

**2. 챕터별 메타데이터 (Chapter Meta)**
- 챕터별 구체적 학습 목표 (1~3줄) 및 포함되어야 할 핵심 키워드(Keywords) 목록
- 챕터 내 구성 비율 구체화 (예: 도입 1p, 개념설명 2p, 코드실습 4p, 트러블슈팅 1p 등)
- 챕터 간 논리적 의존성 및 코드 재사용 내역 명시 (예: "CH03 실습은 CH02의 `data_loader.py`를 재사용함")

**3. 페이지 배분 매트릭스 (Paging Matrix)**
```text
[Part 1. 기본기] CH01 (15p) / CH02 (20p)
[Part 2. 실습] CH03 (30p) / CH04 (25p)
[부록] 에러 가이드 (10p)
합계: 100p (목표 초과 절대 불가)
```

### 🔍 목차 검증 에이전트 체크리스트
목차가 책의 내비게이션 역할을 제대로 수행하는지 점검합니다.

1.  **페이지 분량 할당 검증:**
    *   100페이지라는 총 분량에 맞추어 각 장(Chapter)과 절(Section)에 할당된 예상 페이지 수가 현실적이고 적절한지 검증합니다.
2.  **학습 흐름(Flow) 구조화 점검:**
    *   각 챕터가 **"도입부 ➔ 핵심 개념 설정 ➔ 단계별 실습 ➔ 트러블슈팅 ➔ 요약"** 의 이상적인 교육 구조를 따르고 있는지 확인합니다.
3.  **누락 요소 검사:**
    *   기획서의 목표와 코드 에이전트가 만든 주요 핵심 로직 중 목차에서 누락된 핵심 내용(Keyword)이 없는지 교차 검증합니다.

---

## 4. [집필 파트 - 초안] 본문 작성 및 통합

### 📝 집필 에이전트 명세 및 산출물
**역할:** 목차에 따라 이론, 텍스트 해설, 다이어그램, 코드 스니펫, 터미널 캡처 등을 유려하게 결합하여 전체 초안 생성

**상세 산출물 (전체 원고 및 통합 파일):**
**1. 챕터별 개별 원고 (`chapters/CH{번호}_{제목}.md`)**
- 목차 뼈대를 바탕으로 이론 설명, 코드 조각(`snippets/`), 테스트 로그(`logs/`)가 유기적으로 결합된 챕터별 초안 원고
- 설명의 강약과 환기를 위한 콜아웃 박스(팁, 주의, 참고 등) 배치

**2. 시각 에셋 폴더 (`assets/`)**
- 챕터별로 삽입될 다이어그램 선언문(Mermaid) 및 렌더링된 이미지 파일 보관소
- 코드 실행 결과나 아키텍처 설명에 대한 기타 스크린샷 캡처본

**3. 최종 통합 병합본 (`book_final.md`)**
- 모든 챕터가 순서대로 병합되어 PDF 또는 전자책으로 즉시 변환 생성기 툴에 넣을 수 있는 형태의 단일 마크다운 문서

### 🔍 최종 구문/집필 검증 에이전트 체크리스트
독자가 읽게 될 최종 결과물의 가독성, 포매팅 규칙, 일관성을 책임집니다.

1.  **볼딩 및 특수문자 띄어쓰기 규칙 강제 검사 (중요):**
    *   볼드체(`**`)를 사용하여 강조할 때, 텍스트 괄호나 쌍따옴표 문구 뒤에 **반드시 공백이 포함**되었는지 정규식 기반 검증을 수행합니다.
    *   *통과 예시:* `**"강조"** 함`, `**한글(영어)** 을`
    *   *실패(수정 요망) 예시:* `**"강조"**함`, `**한글 (영어)**을`
2.  **톤앤매너 및 문맥 검증:**
    *   사전에 정의된 문체(예: '하십시오' 체, 멘토링 스타일 등)가 일관되게 적용되었는지, 맥락이 끊기거나 어색한 번역투 문장이 없는지 검토합니다.
3.  **시각 자료 및 설명의 정합성:**
    *   삽입된 다이어그램, 팁, 노트 박스 등이 텍스트 흐름에 맞게 적절한 위치에 들어갔는지 검사합니다.
    *   본문의 코드 설명이 `snippets/` 기반 파일 내용이나 변수명/줄 번호와 정확히 일치하는지 확인합니다.
4.  **용어 통일성 점검:**
    *   책 전반에 걸쳐 동일한 개념을 다르게 부르지 않는지(예: 매개변수 vs 인자, 함수 vs 메서드, 벡터 DB vs 벡터DB) 전문 용어의 일관성을 검수합니다.
