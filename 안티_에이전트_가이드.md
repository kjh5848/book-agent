# 안티 에이전트 가이드 (100페이지 이하 기술서 & 실습)

## Context

`claude_agent_guide.md` 기반으로 기술서 집필 에이전트 시스템 anti_v1을 구축한다.
안티그래비티는 자동 파이프라인이 없으므로 **수동 모드 전용** 으로 운영한다.

핵심 원칙:

- 본 시스템은 **안티그래비티(Antigravity)** 전용이며, `.agents/` 하위에 모든 에이전트·스킬을 관리한다
- 스킬은 레지스트리에서 관리, 프로젝트마다 조합
- 스킬 구조는 **하이브리드**: 범용 규칙은 카테고리 통합, 언어별 규칙은 분리
- 각 에이전트는 **단일 독립 에이전트** 로 동작하며, 사용자가 직접 호출하고 리뷰 후 승인하면 다음 단계로 진행
- `.claude/` 하위의 클로드 코드 에이전트·스킬은 터치하지 않고 완전 분리 운영

---

## 전체 흐름 (마스터 파이프라인)

```text
[Phase 1] 기획 및 구조화 (plan.md 생성 및 검증)
   ↓ (사용자 피드백 & 기획안 승인)
[Phase 2] 실습 예제 코드 생성 및 물리적 검증 (examples/ 코드, tech_plan.md, README.md 등)
   ↓ (사용자 코드 실행 승인)
[Phase 3] 최종 목차(TOC) 확정 (TOC.md)
   ↓ (사용자 목차 승인)
[Phase 4] 전체 본문 집필 및 자동 조판 (chapters/CH*.md)
   ↓ (집필 완료)
[Phase 5] 시스템 회고 및 능동적 워크플로우/스킬 업데이트 (retrospective.md)
```

> **입력 조건**: 작가가 `outline/` 폴더에 자유 형식의 초안(아이디어, 메모, 러프 목차 등)을 넣으면 `/write-book-master` 커맨드를 통해 파이프라인이 시작된다.

---

## 전체 구조

```
집필에이전트-anti/
├── 안티_에이전트_가이드.md             ← 이 파일 (마스터 가이드)
│
├── .agents/                          ← ★ 안티그래비티 에이전트 코어 (통합 관리)
│   ├── workflows/                    ← 마스터 워크플로우 정의 파일들
│   │   └── write-book-master.md      ← 전체 집필 파이프라인 (단일 스크립트 기반)
│   │
│   └── skills/                       ← 에이전트가 참조하는 기능별 스킬(지식) 카탈로그
│       ├── book_registry/            ← 전체 스킬 카탈로그
│       ├── book_writing/             ← 페르소나, 문체, 구조 규칙
│       ├── book_visual/              ← 구조도(Mermaid), 이미지 캡처 규칙
│       ├── book_planning/            ← 기획안(plan.md) 및 기획 검증 규칙
│       ├── book_verification/        ← 본문 교정, 볼딩, 맥락 검증 규칙
│       └── book_code_python/         ← 대상 도서(파이썬) 코딩 및 로깅 규칙
│
├── anti_vX_book/                     ← 프로젝트(버전별) 작업 디렉토리 (ex. anti_v2_book)
│   ├── outline/                      ← 작가 최초 초안(draft) 및 목차 산출물(TOC.md)
│   ├── plan/                         ← 에이전트 기획 산출물 (plan.md)
│   ├── examples/                     ← 코드 에이전트 실습 예제 및 (tech_plan.md, README.md)
│   ├── chapters/                     ← 집필 에이전트 본문 챕터 마크다운 산출물
│   ├── assets/                       ← 캡처 사진 및 시각 에셋
│   └── review/                       ← 최종 검증 보고서 및 회고록(retrospective)
└── ...
```
```

> **버전 관리 원칙**: 각 `anti_vN/` 은 에이전트·스킬·프로젝트 산출물을 모두 포함하는 **자기 완결 패키지** 이다.
> v2를 만들 때는 `anti_v1/` 을 복사한 뒤 에이전트·스킬을 개선한다.

---

## 수동 운영 흐름 (Phase별 승인 게이트)

| Phase | 에이전트 | 사용자 액션 | 산출물 |
|---|---|---|---|
| 0. 초안 | — | 작가가 `outline/`에 초안 배치 | 러프 초안 |
| 1. 기획/회고 | `planning-agent` → `planning-verifier` ↔ **리뷰/피드백 루프** | 기획 내용 집중 검증 및 피드백 반영 후 최종 승인 | `plan/plan.md` |
| 2. 코드 | `code-agent` → **리뷰/승인** → `code-verifier` | 코드 실행 확인 후 승인 | `examples/CH*/` |
| 3. 목차 | `toc-agent` → **리뷰/승인** → `toc-verifier` | 목차 구조 확인 후 승인 | `outline/TOC.md` |
| 4. 집필 | `writing-agent` → **리뷰/승인** → `writing-verifier` | 챕터별 검토 후 승인 | `chapters/CH*.md` |
| 5. 병합 | — | **승인 후** 병합 실행 | `book_final.md` |
| 6. 회고 | `retrospective` | 개선 제안 검토 | `review/retrospective.md` |

---

## [코드 파이프라인]

## 각 Phase별 상세 명세

### Phase 1: 기획 및 구조화

#### 산출물: `plan/plan.md` 폴더의 작가 초안 (자유 형식: 아이디어 메모, 러프 목차, 참고자료 등)

> 작가는 "이런 책을 만들고 싶다"는 수준의 러프한 초안만 제공한다.
> 초안 구체화 에이전트가 이를 읽고 정형화된 기획 문서 그룹으로 변환한다.

#### 초안 구체화 에이전트 산출물: `plan.md` 등 기획 문서 그룹

**1. 설계서 (Project Blueprint)**
- 독자 페르소나 (대상 독자, 사전 지식 수준, 필수 선수 환경)
- 챕터별 학습 목표 (Learning Objective) 및 최종 달성 기준
- 기술 스택 선정 및 명확한 버전 명세 (예: Python 3.11, LangChain 0.2)
- 실습 전 체크리스트 및 사전 준비 작업 가이드 (Door-opening guide)
- **[추가 필수 항목] 실습 진행 방식 (Learning Style):**
  - 독자가 코드를 바닥부터 직접 타이핑(Type-along)하는 방식인지, 아니면 제공된 깃허브 저장소를 클론(Clone)하여 특정 경로의 코드를 복사/붙여넣기 하거나 수정하는 방식인지 명시
- **[추가 필수 항목] 인프라 구성 방식 (Infrastructure):**
  - 가상환경(venv)만 사용하는지, 아니면 특정 DB나 서비스를 도커(Docker) 컨테이너로 띄워서 실습하는지, 어느 챕터에서 도커가 도입되는지 명시

**2. 아키텍처 및 다이어그램 (Architecture & Visuals)**
- 전체 시스템 구성도 (Mermaid 포맷)
- 챕터별 핵심 개념 ↔ 코드 모듈 매핑 테이블
- 시스템/모듈 간 의존성 그래프
- 시나리오별 실행 흐름도 및 데이터 처리 흐름도(Data Flow Diagram)

**3. 환경 명세 및 예외 기획 (Environment & Edge Cases)**
- 필수 환경 변수(.env) 목록 및 획득 방법 가이드
- 연동할 외부 API/서비스 목록 (무료/유료 여부, 요금제 한도 등)
- 지원 OS 범위 (macOS / Windows / Linux) 및 환경별 특이사항
- 실습 중 독자가 겪을 수 있는 주요 실패 시나리오 및 대응 방안 기획

**4. 분량 및 구성 계획 (Paging Plan)**
- 챕터 수 및 예상 페이지 상세 배분 (합계 100페이지 이하 철저 준수)
- 이론 vs 실습 비율 기획 (예: 이론 30%, 실습 70%)

> 초안에 명시되지 않은 항목은 작가의 의도를 추론하여 합리적 기본값으로 채운다.
> 채운 항목에는 `[자동 추론]` 태그를 붙여 작가가 검토할 수 있도록 한다.



### Phase 2: 실습 코드 구현 및 물리적 검증

#### 산출물: `examples/CH{번호}_{제목}/` 및 메타데이터

**1. 프로젝트 구조**

각 챕터의 실습 디렉토리 구성 시 수동으로 폴더와 파일을 만들지 마십시오.
**반드시 아래의 보일러플레이트 자동 생성 스크립트를 먼저 실행**하여 기본 뼈대를 구축한 뒤, `plan.md` 등 기획서를 기반으로 내용을 채워 넣으세요.

```bash
# 디렉토리 생성 및 tech_plan.md, README.md 등 뼈대 자동 생성
python3 .agents/skills/book_code_python/scripts/init_example_dir.py --chapter "02" --name "rag_basic" --out "anti_v2_book/examples"
```

위 스크립트를 통해 생성되는 필수 파일은 다음과 같습니다:
- `tech_plan.md` — 해당 챕터 예제 코드의 기술 설계서 (요구사항, 유스케이스, 다이어그램, 아키텍처 구조 명세)
- `README.md` — 학생용 실행 가이드
- `chapter_spec.md` — 집필 에이전트 전용 명세 (스크립트 실행 후 수동 추가 생성)
- `requirements.txt` — 파이썬 의존성 파일
- `.env.example` — API 키 등 환경 변수 템플릿 (해당하는 경우 직접 추가)

> 학생이 보는 구조이므로 파일/폴더는 최소한으로 유지한다.
> 실습에 샘플 데이터가 필요한 경우 코드 내에서 직접 생성하거나 README에 다운로드 링크를 제공한다.

**tech_plan.md 생성 항목 (코드 개발 설계도)**
- 요구사항 정의 (어떤 문제를 해결하는 코드인지)
- 아키텍처 및 시스템 처리 흐름도 (Mermaid)
- 파일별 역할 상세 명세
- 테스트/검증 계획

**README 생성 항목 (학생용)**

- 챕터 목적 및 학습 목표
- 예제 코드 기획 의도 및 전체 구조 설명 (어떻게 만들었는지, 어떤 구조인지 초보자가 이해하기 쉽도록)
- 소속된 각 파일별 역할 상세 설명
- 실행 환경 (런타임 버전, OS) 및 설치 방법 (macOS / Windows 분리)
- 실행 순서 (단계별, 순서대로 따라하면 반드시 성공하도록 구체적으로)
- 예상 실행 결과 (원론적인 설명이나 요약은 절대 금지하며, 사용자가 직접 복사/비교할 수 있도록 **터미널 출력 전문(Raw Log)을 코드블럭 형태로 100% 삽입**할 것)
- 실습 결과 스크린샷(캡처 사진) 가이드 (작성자는 이 부분을 보고 캡처를 진행합니다)
  - 캡처가 들어갈 정확한 마크다운 위치를 지정 (예: `<!-- [캡쳐 사진 삽입 위치: Type C 터미널 성공 로그 전체 화면] -->`)
  - **[중요]** 각 실습 스텝별로 캡처해야 할 화면의 종류(예: "에러가 발생한 첫 번째 단계 콘솔 화면", "정상 출력된 마지막 단계 웹/콘솔 화면")를 구체적으로 명시할 것.
- 전체 구조 다이어그램 (Mermaid)

**chapter_spec.md 생성 항목 (집필 에이전트 전용)**

> 집필 에이전트는 README가 아닌 `chapter_spec.md`를 원천 입력으로 사용한다.
> 코드 에이전트가 코드 작성 완료 후 반드시 함께 생성한다.

```markdown
# CH{번호} 집필 명세

## 1. 챕터 섹션 구조
- ## 1. {섹션명}: {설명할 내용 한 줄 요약}
- ## 2. {섹션명}: {설명할 내용 한 줄 요약}
- ## N. 정리하며

## 2. 코드-섹션 매핑
| 섹션 | src/ 파일 | 설명할 코드 범위 |
|------|-----------|----------------|
| ## 1. | main.py | load_document() 함수 |
| ## 2. | retriever.py | search() 전체 |

## 3. 개념 설명 힌트 (Why)
- load_document()를 사용하는 이유: ...
- 청크 사이즈를 500으로 설정한 이유: ...

## 4. 핵심 용어
- RAG (Retrieval-Augmented Generation): ...
- 벡터 임베딩(Vector Embedding): ...

## 5. Mermaid 다이어그램 초안
{집필 에이전트가 참고할 아키텍처 다이어그램}

## 6. 챕터 연결
- 이전 챕터에서 가져오는 개념: ...
- 다음 챕터로 넘기는 개념: ...
```

**코드 규칙**

- 네이밍: `snake_case`(변수/함수), `PascalCase`(클래스), `UPPER_SNAKE_CASE`(상수)
- 모든 함수에 한국어 docstring + 타입 힌트
- 복잡한 로직 앞 블록 주석 필수
- 코드 블록마다 `# Input / Process / Output` 구간 주석
- 에지 케이스 에러 메시지는 독자 친화적 한국어로 작성
- `requirements.txt` 패키지 버전 반드시 고정

#### 물리적 코드 검증 체크리스트

1. **실행 가능성 및 재현성 (Reproducibility) 테스트:**
   - 가상환경(venv) 신규 생성 후 `requirements.txt`만으로 설치가 성공하는지 검증한다.
   - `main.py` 정상 실행 및 `logs/console_output.txt`가 일치하는지 확인한다.
2. **에러 처리 검증:**
   - 필수 환경 변수 누락 시 친절한 에러 메시지가 출력되는지 확인한다.
   - 잘못된 파일 경로 입력 시 크래시 없이 처리되는지 확인한다.
3. **동기화 검증:**
   - 원고용 코드 스니펫과 실제 구동 소스 코드가 100% 일치하는지 확인한다.
   - `chapter_spec.md`의 코드-섹션 매핑이 실제 `src/` 파일과 일치하는지 확인한다.
4. **다이어그램 문법 검사:**
   - Mermaid 다이어그램 작성 시, 괄호 `()`가 포함된 텍스트 노드가 반드시 쌍따옴표 `""`로 감싸져 있는지 확인한다.
5. **주석 및 코드 규칙 검사:**
   - 코드 내의 모든 주석이 한국어로 작성되었는지 확인한다.
   - Windows / macOS 양쪽 실행 명령어가 검증되었는지 확인한다.
- [ ] 산출물: `verify_report.md` (수정 내역 + 최종 상태)

---

### Phase 3: 최종 목차(TOC) 확정

#### 필요 입력 데이터

- `plan.md` (기획서)
- `examples/*/README.md` (각 챕터 코드 산출물)

#### 산출물: `outline/TOC.md`

**1. 전체 구조도 (Table of Contents)**
- 대분류(Part) - 중분류(Chapter) - 소분류(Section) 계층 구조
- 각 챕터 하위의 주요 섹션 요약(Abstract) 및 각 섹션별 예상 페이지 수

**2. 챕터별 메타데이터 (Chapter Meta)**
- 챕터별 구체적 학습 목표 (1~3줄) 및 포함되어야 할 핵심 키워드(Keywords) 목록
- 챕터 내 구성 비율 구체화 (예: 도입 1p, 개념설명 2p, 코드실습 4p, 트러블슈팅 1p 등)
- 챕터 간 논리적 의존성 및 코드 재사용 내역 명시 (예: "CH03 실습은 CH02의 `data_loader.py`를 재사용함")

**3. 페이지 배분 매트릭스 (Paging Matrix)**
```text
[Part 1. 기본기] CH01 (15p) / CH02 (20p)
[Part 2. 실습] CH03 (30p) / CH04 (25p)
[부록] 에러 가이드 (10p)
합계: 100p (목표 초과 절대 불가)
```

#### 목차 검증 체크리스트

1. **페이지 분량 할당 검증:**
   - 챕터 수 × 평균 페이지가 100페이지 이하인지 검증한다.
   - 각 장(Chapter)과 절(Section)에 할당된 예상 페이지 수가 현실적이고 적절한지 검증한다.
2. **학습 흐름(Flow) 구조화 점검:**
   - 도입부 → 심화 순서로 학습 곡선이 자연스러운지 확인한다.
   - 각 챕터가 **"도입부 ➔ 핵심 개념 설정 ➔ 단계별 실습 ➔ 트러블슈팅 ➔ 요약"** 의 이상적인 교육 구조를 따르고 있는지 확인한다.
3. **누락 요소 검사:**
   - 각 챕터가 단독으로도 의미가 있는지 확인한다.
   - 첫 챕터에서 독자가 실행 결과를 볼 수 있는지(조기 성취감) 확인한다.
   - 기획서의 목표와 코드 에이전트가 만든 주요 핵심 로직 중 목차에서 누락된 핵심 내용이 없는지 교차 검증한다.

---

### Phase 4: 전체 본문 집필 및 자동 조판

#### 필요 입력 데이터

**1. 목차 (`outline/TOC.md`)**
- 목차 에이전트가 생성한 전체 목차 및 해당 챕터의 섹션 구조, 학습 목표

**2. 코드 에이전트 산출물 (집필 원천 재료)**
- `examples/CH{번호}_{제목}/chapter_spec.md` — 집필 에이전트 전용 명세
- `examples/CH{번호}_{제목}/src/` — 본문에 삽입할 실제 코드

> 집필 에이전트는 `chapter_spec.md` + `src/` 를 원천 입력으로 사용한다.
> README는 학생용이며 집필 에이전트의 입력이 아니다.

**3. 이전 챕터 요약 (자동 생성)**
- 직전 챕터 집필 완료 시 현재 에이전트가 `chapters/CH{번호}_{제목}_summary.md` 를 자동 생성
- 내용: 핵심 개념 3줄, 등장한 코드 모듈명, 다음 챕터로 넘기는 연결 문장
- 첫 번째 챕터는 이전 챕터 요약 없이 시작

#### 본문 산출물

**1. 챕터별 개별 원고 (`chapters/CH{번호}_{제목}.md`)**
- 에이전트는 `# 1. 개념 설명`, `# 2. 실습 진행` 등의 챕터 구조를 처음부터 타이핑하지 않습니다.
- **반드시 아래 스크립트를 실행하여 아웃라인이 완벽히 잡힌 빈 마크다운 문서를 먼저 분리 생성**한 뒤, 내용을 채워 넣습니다.
```bash
python3 .agents/skills/book_writing/scripts/init_chapter_md.py --chapter "01" --title "RAG 기초" --out "anti_v2_book/chapters"
```
- 목차 뼈대를 바탕으로 이론 설명, 코드 조각, 테스트 로그가 유기적으로 결합되도록 챕터를 작성합니다.

**2. 시각 에셋 폴더 (`assets/`)**
- 챕터별 다이어그램 선언문(Mermaid) 및 렌더링된 이미지

**3. 최종 통합 병합본 (`book_final.md`)**
- 모든 챕터가 순서대로 병합된 단일 마크다운 파일

**챕터 구조 (필수 준수)**

```markdown
# N. 챕터 제목

도입 (1~2문장)

## 1. 개념 설명

{Mermaid 다이어그램}

{개념 설명}

## 2. 실습

{코드 스니펫 파일명 명시}
{코드 블록}

#### 코드 워크플로우 (Code Workflow)

1. **입력(Input)**: ...
2. **처리(Process)**: ...
3. **출력(Output)**: ...

## 3. 심화 / 에러 대응

{실행 시 터미널 출력 결과 텍스트 삽입}

## N. 정리하며

- **핵심 1**: 설명
- **핵심 2**: 설명
- **핵심 3**: 설명
```

**박스 스타일**

```markdown
> **팁: 제목**
> 유용한 정보

> **주의: 제목**
> 잠재적 실수

> **경고: 제목**
> 데이터 손실 위험

> **참고: 제목**
> 부가 설명
```

**기타 규칙**

- 챕터 말미에 다음 챕터 연결 문장 1줄 포함
- 이미지 캡션: `*그림 N-M: 설명*` (이탤릭체)

---

#### 본문 자체 검증 체크리스트

#### 문체 검증

- [ ] 하십시오체 일관 유지 (`~합니다`, `~입니다`)
- [ ] 추측성 표현 없음 (`~인 것 같다`, `~해 보인다` 금지)
- [ ] 상투적 비유 없음 (`긴 여정`, `마법 같은`, `첫걸음` 금지)
- [ ] 이모지 본문 사용 없음

#### 볼딩 처리 검증

- [ ] `**볼딩**` 앞뒤 공백 포함 여부
  - `**"볼딩"** 이라 합니다.` (O) / `**"볼딩"**이라 합니다.` (X)
  - `**볼딩(English)** 은 ...` (O) / `**볼딩(English)**은 ...` (X)
- [ ] 볼딩 남용 없음 (문장당 1~2개 이하)

#### 맥락 검증

- [ ] 이전 챕터에서 소개한 개념을 재설명 없이 참조하는가
- [ ] 챕터 간 용어가 통일되어 있는가 (예: "벡터 DB" vs "벡터DB" 혼용 금지)
- [ ] 영문 병기가 최초 1회만 등장하는가

#### 구조 검증

- [ ] 모든 챕터 마지막 섹션이 `## N. 정리하며` 형식인가
- [ ] 모든 코드 블록 아래에 코드 워크플로우 섹션이 있는가
- [ ] Mermaid 문법: `flowchart` 키워드, 괄호 포함 노드는 쌍따옴표 처리

#### 동기화 검증

- [ ] 본문 코드 블록과 `examples/.../src/` 실제 코드가 일치하는가
- [ ] 터미널 실행 결과가 실제 실행 결과와 일치하는가
- [ ] 이미지 경로(`assets/...`)가 실제 에셋 파일과 일치하는가
- [ ] README 실습 순서와 본문 실습 순서가 일치하는가

#### 톤앤매너 검증

- [ ] 사전에 정의된 문체(하십시오체, 멘토링 스타일)가 일관되게 적용되었는가
- [ ] 맥락이 끊기거나 어색한 번역투 문장이 없는가

#### 분량 검증

- [ ] 전체 챕터 합산 분량이 100페이지 이하인가
- [ ] 단일 챕터가 20페이지를 초과하지 않는가

---



---

## Phase 5: 시스템 회고 및 능동적 업데이트

**실행 시점**: 집필 완료(Phase 4) 이후, 마스터 워크플로우 Phase 5 진행 시
**입력**: 전체 산출물 및 **사용자 피드백 (인터랙션 필수)**
**출력**: `review/retrospective.md` 및 시스템 로직(스킬/워크플로우) 능동 업데이트 반영 내역

**분석 및 동작 항목**:

1. **사용자 인터랙션(필수)**: `notify_user`를 통해 사용자에게 "작업 과정에서 부족했던 부분이나 워크플로우/스킬 문서에 추가/개선했으면 하는 내용"을 직접 질문하고 피드백을 수합합니다.
2. 각 Phase별 소요 시간, 반복 오류 패턴, 사용자의 피드백 내용을 종합 분석합니다.
3. **능동적 스킬/워크플로우 개선**: 분석 결과를 바탕으로 워크플로우(`.agents/workflows/`) 파일이나 스킬 문서(`.agents/skills/`)를 제안에 그치지 않고 **직접 수정(`replace_file_content`)**합니다.
4. 모든 프롬프트 수정 내역과 다음 회고 및 배울 점(lessons learned)을 `review/retrospective.md`에 최종 기록하여 보고합니다.

> Phase 5 단계를 통해 에이전트는 피드백을 수합하고 단순 제안을 넘어, **시스템(스킬/워크플로우)을 스스로 레벨업시키는 능동적 역할**을 수행합니다.
